# Vite (Windows) → API en WSL: usa 127.0.0.1 si el backend escucha en 0.0.0.0:8787 (reenvío WSL2).
# La IP 192.168.x.x cambia y suele dar ETIMEDOUT; solo úsala si 127.0.0.1 no te funciona (`wsl -e hostname -I`).
VITE_PROXY_API=http://127.0.0.1:8787
