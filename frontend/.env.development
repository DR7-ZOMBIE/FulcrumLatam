# Call API directly from the browser (CORS). Reduces ERR_CONNECTION_RESET vs tunneling via Vite proxy.
VITE_API_URL=http://192.168.1.3:8787
# Fallback when VITE_API_URL is empty (relative /api)
VITE_PROXY_API=http://192.168.1.3:8787
